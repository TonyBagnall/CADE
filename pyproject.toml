[build-system]
requires = ["setuptools>61", "wheel", "toml", "build"]
build-backend = "setuptools.build_meta"

[project]
name = "cade"
version = "0.1"
description = "CADE: Context-Aware Differentiable Elastic Alignment"
readme = { file = "README.md", content-type = "text/markdown" }
authors = [{ name = "Christopher Holder" }]
requires-python = ">=3.10"

dependencies = [
    "aeon@git+https://github.com/aeon-toolkit/aeon@soft-msm-experiments#egg=aeon",
    "matplotlib",
    "seaborn",
    "tsml-eval@git+https://github.com/time-series-machine-learning/tsml-eval@main#egg=tsml-eval",
    "python-dotenv"
]

[project.optional-dependencies]
dev = [
  "black",
  "flake8",
  "mypy",
  "pytest",
  "pytest-cov",
  "pytest-xdist",
  "pytest-timeout",
  "pytest-rerunfailures",
  "pre-commit"
]

pytorch = [
  "torch>=2.3"
]

jax = [
  "jax>=0.4.31",
  "jaxlib>=0.4.31"
]

tensorflow = [
  "tensorflow>=2.15; platform_system == 'Linux' and platform_machine == 'x86_64' and python_version < '3.13'",
  "tensorflow-macos>=2.15; platform_system == 'Darwin' and platform_machine == 'arm64' and python_version < '3.13'"
]

all = [
  "torch>=2.3",
  "jax>=0.4.31",
  "jaxlib>=0.4.31",
  "tensorflow>=2.15; platform_system == 'Linux' and platform_machine == 'x86_64' and python_version < '3.13'",
  "tensorflow-macos>=2.15; platform_system == 'Darwin' and platform_machine == 'arm64' and python_version < '3.13'"
]

[tool.setuptools]
packages = ["cade"]

[tool.ruff.lint]
select = ["D"]

[tool.ruff.lint.per-file-ignores]
"**/tests/**/*.py" = ["D100", "D103", "D104"]

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.flake8]
per-file-ignores = [
    "cade/jax/tests/test_*.py:E402",
]

[tool.pytest.ini_options]
testpaths = [
    "cade"
]
addopts = '''
    --doctest-modules
    --durations 20
    --timeout 600
    --showlocals
    --dist worksteal
    --reruns 2
    --only-rerun "crashed while running"
    -v
'''
filterwarnings = '''
    ignore::UserWarning
    ignore:numpy.dtype size changed
    ignore:numpy.ufunc size changed
'''
